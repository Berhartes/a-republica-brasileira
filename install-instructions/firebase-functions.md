# Firebase Functions Installation Instructions

You need to install the following packages to resolve the Firebase Functions issues:

```bash
pnpm add firebase-functions firebase-functions-test
```

Once installed, you'll need to run the build command again:

```bash
pnpm run build
```

These packages are required for the Cloud Functions functionality in your project.
